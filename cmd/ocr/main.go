// Command ocr runs the end-to-end OCR pipeline on images.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "ocr: pipeline not implemented yet (see docs/ROADMAP.md phase 3)")
	os.Exit(2)
}
