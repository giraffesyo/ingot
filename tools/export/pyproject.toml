[project]
name = "ocr-export"
version = "0.0.1"
description = "Exports reference ONNX models + ORT outputs into testdata/ for conformance tests"
requires-python = ">=3.11,<3.13"
dependencies = [
  "torch",
  "torchvision",
  "onnx",
  "onnxruntime",
  "numpy",
]
